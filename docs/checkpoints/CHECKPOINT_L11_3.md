# CHECKPOINT L11.3 — Decision3 lift (institutional preparation)

Adds:
- `Decision3` tri-state: BLOCK/HOLD/ACT
- `decision3` = lifted OS2 `decision`
- Proof that OS2 core never outputs `BLOCK` (`L11_3_no_block`)
- Lifted obligation lemmas (`L11_3_act`, `L11_3_hold`)

Build:
```bash
cd lean
lake build
```
